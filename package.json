{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "turbo": "^2.10.9"
  },
  "name": "with-vite-react",
  "devEngines": {
    "packageManager": {
      "name": "npm",
      "version": "11.3.0"
    }
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
